ARG GOBEL_API_IMAGE_NAME=${GOBEL_MYSQL_IMAGE_NAME}
ARG GOBEL_API_IMAGE_TAG=${GOBEL_MYSQL_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_API_IMAGE_NAME}:${GOBEL_API_IMAGE_TAG}

RUN apk add curl