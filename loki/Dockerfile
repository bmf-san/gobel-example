ARG GOBEL_LOKI_IMAGE_NAME=${GOBEL_LOKI_IMAGE_NAME}
ARG GOBEL_LOKI_IMAGE_TAG=${GOBEL_LOKI_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_LOKI_IMAGE_NAME}:${GOBEL_LOKI_IMAGE_TAG}