server.name: kibana
server.host: "0"
elasticsearch.hosts: [ "http://${GOBEL_ELASTICSEARCH_CONTAINER_NAME}:${GOBEL_ELASTICSEARCH_CONTAINER_HOST}" ]
xpack.monitoring.ui.container.elasticsearch.enabled: true
elasticsearch.username: ${GOBEL_ELASTICSEARCH_USERNAME}
elasticsearch.password: ${GOBEL_ELASTICSEARCH_PASSWORD}