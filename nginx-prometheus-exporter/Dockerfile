ARG GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_NAME=${GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_NAME}
ARG GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_TAG=${GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_NAME}:${GOBEL_NGINX_PROMETHEUS_EXPORTER_IMAGE_TAG}