ARG GOBEL_BLACKBOX_EXPORTER_IMAGE_NAME=${GOBEL_BLACKBOX_EXPORTER_IMAGE_NAME}
ARG GOBEL_BLACKBOX_EXPORTER_IMAGE_TAG=${GOBEL_BLACKBOX_EXPORTER_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_BLACKBOX_EXPORTER_IMAGE_NAME}:${GOBEL_BLACKBOX_EXPORTER_IMAGE_TAG}