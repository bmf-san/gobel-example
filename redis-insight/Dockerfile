ARG GOBEL_REDIS_INSIGHT_IMAGE_NAME=${GOBEL_REDIS_INSIGHT_IMAGE_NAME}
ARG GOBEL_REDIS_INSIGHT_IMAGE_TAG=${GOBEL_REDIS_INSIGHT_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_REDIS_INSIGHT_IMAGE_NAME}:${GOBEL_REDIS_INSIGHT_IMAGE_TAG}