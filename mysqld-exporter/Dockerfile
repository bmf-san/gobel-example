ARG GOBEL_MYSQLD_EXPORTER_IMAGE_NAME=${GOBEL_MYSQLD_EXPORTER_IMAGE_NAME}
ARG GOBEL_MYSQLD_EXPORTER_IMAGE_TAG=${GOBEL_MYSQLD_EXPORTER_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_MYSQLD_EXPORTER_IMAGE_NAME}:${GOBEL_MYSQLD_EXPORTER_IMAGE_TAG}