ARG GOBEL_GRAFANA_IMAGE_NAME=${GOBEL_GRAFANA_IMAGE_NAME}
ARG GOBEL_GRAFANA_IMAGE_TAG=${GOBEL_GRAFANA_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${GOBEL_GRAFANA_IMAGE_NAME}:${GOBEL_GRAFANA_IMAGE_TAG}