ARG PLATFORM=${PLATFORM}
ARG GOBEL_PYROSCOPE_IMAGE_NAME=${GOBEL_PYROSCOPE_IMAGE_NAME}
ARG GOBEL_PYROSCOPE_IMAGE_TAG=${GOBEL_PYROSCOPE_IMAGE_TAG}

FROM --platform=${PLATFORM} ${GOBEL_PYROSCOPE_IMAGE_NAME}:${GOBEL_PYROSCOPE_IMAGE_TAG}

CMD ["server"]