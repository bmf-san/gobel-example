ARG GOBEL_LOGSTASH_IMAGE_NAME=${GOBEL_LOGSTASH_IMAGE_NAME}
ARG GOBEL_LOGSTASH_IMAGE_TAG=${GOBEL_LOGSTASH_IMAGE_TAG}

FROM ${GOBEL_LOGSTASH_IMAGE_NAME}:${GOBEL_LOGSTASH_IMAGE_TAG}

RUN logstash-plugin install logstash-output-slack